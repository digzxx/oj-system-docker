# ----------------------------------------------------
# 仅用于开发环境 (Dev Environment)
# ----------------------------------------------------
# 使用 Node 基础镜像
FROM node:20-alpine

# 设置工作目录
WORKDIR /app

# 复制依赖文件，利用 Docker 缓存
# 复制 package-lock.json 是为了保证安装版本一致性
COPY package*.json ./ 

# 安装所有依赖
RUN npm install

# 暴露前端开发服务器端口
EXPOSE 5173 

# 默认启动命令 (在 docker-compose.yaml 中已被覆盖为 npm run dev)
CMD ["npm", "run", "dev"]